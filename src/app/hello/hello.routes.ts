import { RouterConfig }         from '@angular/router';
import { Hello }                from './hello/hello.component';

export const helloRoutes: RouterConfig = [
  { path: '', component: Hello }
];
